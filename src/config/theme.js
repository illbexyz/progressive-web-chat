import { indigo500, cyan700 } from 'material-ui/styles/colors'

export const primaryColor = indigo500
export const accentColor = cyan700

const theme = {
  palette: {
    primary1Color: primaryColor,
    accent1Color: accentColor,
  },
  appBar: {
    height: 56,
  },
}

export default theme
